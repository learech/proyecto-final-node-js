<div class="title">
    <h4 style="background-color:rgb(0, 0, 0);">Please enter a valid Email address, we will send you a password reset email.</h4>
</div>
<div class="container"> 
    {{!-- <div class="box">
        <h5 class="message"> {{message}}</h5>
    </div><br><br> --}}
    <div class="box">
        <div class="row d-flex justify-content-center">
            {{!-- Form --}}
            <form class="form" method="post" action="/api/email/reset-info"> 
                <div class="form-group row">
                    <label for="email" class="col-sm-2 col-form-label">Email address</label>
                    <div class="col-sm-10">
                    <input type="email" class="form-control" id="email" name="email" placeholder="Your Email address" required ><br>
                    <small id="emailHelp" class="form-text text-muted">We'll never share your email with anyone else.</small>
                    </div>
                </div>
                <br>
                <div class="form-group row">
                    <div class="col-sm-10">
                        <button type="submit" class="btn btn-primary">Send </button>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>
